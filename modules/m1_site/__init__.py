# m1_site package marker (Pre-28 remediation).
# Present so modules.m1_site can be imported as a package; the modules within
# are also usable via direct-file reference + sys.path insert (legacy pattern).
